import { DrawerAutosizeDirective } from './drawer-autosize.directive';

describe('DrawerAutosizeDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerAutosizeDirective();
    expect(directive).toBeTruthy();
  });
});
